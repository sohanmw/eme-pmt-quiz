{
  "name": "quiz-host",
  "version": "1.0.0",
  "description": "Self-hosted, real-time, Kahoot-style quiz game. No accounts, no paid services, no database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "tunnel": "./bin/cloudflared tunnel --url http://localhost:3001"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  }
}
